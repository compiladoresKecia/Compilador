 start
 	int a, b;
 	int result;
 	float x,total;
 
 	a = 2;
 	x = 0.1;
 	scan (b);
 	scan (x);
 	result = (a*b + 1) / 2;
 	print ("Resultado: ");
 	print (result);
 	print ("Total: ");
 	total = result / x;
 	print ("Total: ");
 	print (total);
 exit